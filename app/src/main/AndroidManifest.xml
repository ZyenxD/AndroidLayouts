<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.neycasilla.androidlayouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Main2Activity" />
        <activity android:name=".activities.RealiteveActivity" />
        <activity android:name=".activities.TableActivity" />
        <activity android:name=".activities.FrameActivity" />
        <activity android:name=".activities.ConstrainActivity" />
        <activity android:name=".activities.weightActivity" />
        <activity android:name=".activities.ListActivity">
        </activity>
        <activity android:name=".activities.AutoCompleteActivity">

        </activity>
    </application>

</manifest>